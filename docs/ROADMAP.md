# Roadmap

- [x] M1 — Pin exact private revisions for all three ecosystem repositories.
- [x] M2 — Run product checks and the installed three-product packed acceptance
  from disposable detached checkouts.
- [x] M3 — Emit private, canonical SHA-256 receipts with artifact digests and
  explicit assurance limits.
- [x] M4 — Compare two lock files and classify product, protocol, schema, and
  acceptance changes before an upgrade.
- [x] M5 — Build a retained, append-only acceptance index from independently
  anchored receipt heads.
- [x] M6 — Publish the upgrade workflow as `v0.2.0` and link its operator entry
  points from Agent Black Box, Sol Ledger, and Evidence Forge.
- [x] M7 — Refresh the lock to the merged adoption revisions, run old→new
  preflight and complete acceptance, then retain both receipt heads in the first
  versioned append-only index.
- [x] M8 — Add a sub-three-second offline demo and an aggregate environment
  doctor so first-time users can see the value before installing the full
  private-repository and Rust toolchain.
- [x] M9 — Harden pre-publication hygiene with failure-safe ephemeral-key
  cleanup, reproducible history/tree secret scanning, environment/session-file
  ignores, and an explicit generated-output audit boundary.
- [x] M10 — Refresh the lock to the three merged public-readiness candidates,
  classify the upgrade without execution, complete the full four-repository
  acceptance run, and append its independently supplied receipt head.
- [x] M11 — Resolve personal-email history non-destructively with four reviewed
  fresh snapshots authored through GitHub noreply, while retaining the original
  private development repositories unchanged.
- [x] M12 — Classify repository relocation, run the complete clean-history
  ecosystem acceptance, and append its externally supplied receipt head.
- [x] M12a — Refresh the clean-history pins to the final review remediations,
  classify the runtime/schema changes without execution, complete full local
  acceptance, and append the independently headed receipt to the retained index.
- [ ] M13 — Obtain explicit approval before changing repository visibility or
  publishing any npm package, crate, deployment, or announcement.
